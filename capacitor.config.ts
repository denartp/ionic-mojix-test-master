import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mojix Coding Challenge base',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
